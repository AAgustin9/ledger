export default function AppFooter() {
  return (
    <footer className="app-footer">
      <span className="mono footer-note">All data stored locally — nothing leaves your device</span>
    </footer>
  );
}
